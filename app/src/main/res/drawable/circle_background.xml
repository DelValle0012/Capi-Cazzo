<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="oval">
            <!-- Define o preenchimento como transparente -->
            <solid android:color="@android:color/transparent" />
            <!-- Define a borda na cor desejada -->
            <stroke
                android:color="@color/vermelhorosa"
                android:width="2dp" /> <!-- Ajuste a largura conforme necessário -->
        </shape>
    </item>
</selector>
